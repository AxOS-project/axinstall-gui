# Axinstall GUI

This is a GUI for the Axinstall backend. It is made with python and GTK.

## 💾 Installation
```bash
 $ meson --prefix=/usr _build
 $ ninja -C _build
 $ cd _build
 $ sudo ninja install
```